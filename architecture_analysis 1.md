# تحليل بنية مشروع مؤسسة الوليد الإنسانية

هذا المستند يمثل تحليلاً معمارياً وهيكلياً شاملاً للمشروع الحالي. الهدف من هذا التحليل هو تفكيك المشروع إلى مكونات (Components) ووظائف (Features) بحيث يسهل عليك مستقبلاً إعادة بنائه باستخدام أي لغة أو إطار عمل آخر (مثل React, Next.js, Vue.js، أو حتى WordPress).

---

## 1. هيكل الصفحات والتوجيه (Routing & Pages)
المشروع الحالي يعتمد على صفحات HTML منفصلة (Multi-Page Application). إذا أردت الانتقال إلى إطار عمل حديث (SPA)، سيتم تحويل هذه الصفحات إلى مسارات (Routes):

- **الصفحة الرئيسية (`/`):** تعرض نظرة عامة (سلايدر، إحصائيات، مجالات، مبادرات، معرض صور، تواصل).
- **طلب المساعدة (`/us`):** نموذج تسجيل بيانات المستفيدين لطلب مساعدة.
- **تقديم التبرع (`/donate`):** نموذج لجمع التبرعات وتوجيه المتبرع لطرق الدفع.
- **صفحات أخرى:** (من نحن، المشاريع، الحالات، سياسة الخصوصية، شروط الاستخدام).

> [!TIP]
> عند الانتقال إلى إطارات عمل مثل Next.js أو React، لن تقوم بإنشاء صفحات HTML منفصلة، بل ستستخدم نظام توجيه (Router) مثل `react-router-dom` أو نظام الـ App Router في Next.js للانتقال بين الصفحات بسلاسة وبدون إعادة تحميل للصفحة.

---

## 2. تفكيك واجهة المستخدم إلى مكونات (UI Components)
في اللغات الحديثة، لا يتم تكرار الكود، بل يتم تقسيمه إلى "مكونات" قابلة لإعادة الاستخدام. بناءً على كود موقعك، إليك المكونات الأساسية التي ستقوم بإنشائها:

### مكونات عامة (Global Components)
- `Navbar`: شريط التنقل العلوي (يحتوي على الشعار والروابط والقائمة الجانبية).
- `Footer`: تذييل الموقع (يحتوي على الروابط القانونية، ومعلومات التواصل، والاعتمادات).
- `FaqModal`: نافذة الأسئلة الشائعة المنبثقة وزر المساعدة العائم.

### مكونات الصفحة الرئيسية (Home Components)
- `HeroSlider`: السلايدر الرئيسي مع خلفيات الصور المتغيرة والنصوص المتحركة.
- `StatsCounter`: مكون العدادات الذي يبدأ بالعد عند التمرير (إحصائياتنا).
- `ServiceCard`: بطاقة الخدمة (مثل: إغاثة الكوارث، تمكين المرأة) - مكون يعاد استخدامه ببيانات مختلفة.
- `GallerySlider`: معرض الصور ذو التمرير اللانهائي (Continuous Scroll).
- `ApCarousel`: دائرة الأرقام والإنجازات.

### مكونات النماذج (Form Components)
- `HelpRequestForm`: نموذج طلب المساعدة (يحتوي على حقول النص، واختيار الدولة، ونوع المساعدة).
- `DonationForm`: نموذج التبرع (يحتوي على تبويبات لاختيار وسيلة الدفع: بنكي، ويسترن يونيون، عملات رقمية).

---

## 3. إدارة البيانات والاتصال (Data Flow & Integrations)

نظامك الحالي يعتمد بشكل أساسي على تكاملين رئيسيين:

### أ. قاعدة البيانات (Firebase Firestore)
أنت تستخدم Firebase لحفظ البيانات قبل تحويل المستخدم للواتساب.
- **مجموعة التبرعات (`donations`):**
  - **الحقول:** `name` (الاسم)، `amount` (المبلغ)، `currency` (العملة)، `nationality` (الجنسية)، `method` (طريقة الدفع)، `date` (التاريخ).
  - **العملية:** يتم استخدام `addDoc` لإنشاء معرف فريد لكل طلب تبرع.
- **مجموعة طلبات المساعدة (`us`):**
  - **الحقول:** `name` (الاسم)، `phone` (الجوال)، `location` (الدولة)، `type` (نوع المساعدة)، `notes` (ملاحظات)، `date` (التاريخ).
  - **العملية:** يتم استخدام `setDoc` مع معرف مكون من (رقم الجوال + طابع زمني) لضمان عدم الحظر في حال تكرار الطلب.

> [!IMPORTANT]
> إذا انتقلت للغة أخرى (مثل PHP/Laravel أو Node.js/Express)، ستقوم باستبدال Firebase بإنشاء جداول (Tables) في قاعدة بيانات SQL (مثل MySQL أو PostgreSQL) وإنشاء واجهات برمجة تطبيقات (APIs) تستقبل هذه البيانات من الواجهة الأمامية.

### ب. توجيه الواتساب (WhatsApp Integration)
- الكود يقرأ البيانات من النموذج.
- يقوم بتجميع رسالة منسقة (Formatted Message) باستخدام رموز ونجوم لجعل النص عريضاً في الواتساب.
- يستخدم `window.location.href = "https://wa.me/..."` لتحويل المستخدم.

---

## 4. الحركة والتصميم (Animations & Styling)
- **ملفات CSS الحالية:** مقسمة بشكل جيد (`global.css`، `components.css`، `pages.css`).
- **الحركات (Animations):** الموقع يعتمد بشكل كبير على الـ `Intersection Observer API` لإضافة كلاس `active` أو `appear` للعناصر عند ظهورها في الشاشة.

> [!TIP]
> في بيئات العمل الحديثة، يمكنك استخدام مكتبات قوية ومبسطة للأنيميشن مثل **Framer Motion** (لـ React) أو استبدال الـ CSS التقليدي بإطار عمل مثل **Tailwind CSS** الذي سيسرع من عملية التصميم ويجعل الكود أكثر نظافة.

---

## 5. خريطة الطريق المقترحة لإعادة البناء (Migration Roadmap)
إذا قررت يوماً ما إعادة بناء الموقع باستخدام إطار عمل حديث (لنفرض **Next.js**)، ستكون خطواتك كالتالي:

1. **إعداد المشروع:** إنشاء مشروع Next.js جديد وتجهيز Tailwind CSS.
2. **بناء التصميم الأساسي (Layout):** إنشاء مكونات الـ Navbar والـ Footer لتعمل في جميع المسارات.
3. **تطوير واجهة المستخدم:** تحويل صفحات HTML الحالية إلى مكونات React (React Components).
4. **تطوير المنطق (Logic):** تحويل منطق السلايدر والعدادات (الموجود في `main.js` حالياً) إلى `Hooks` مثل `useState` و `useEffect`.
5. **ربط قاعدة البيانات:** نقل كود Firebase ليعمل من خلال الواجهة الخلفية لـ Next.js (Server Actions أو API Routes) لزيادة الأمان وإخفاء مفاتيح الـ API.
6. **النشر:** رفع المشروع على خادم حديث مثل Vercel.
